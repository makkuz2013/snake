# Snake

This is just the source code, to play it, copy the repo into a folder and run main.py with Python. Or open the terminal and do "python main.py" or "python3 main.py" if you're on UNIX (Linux or MacOS).
